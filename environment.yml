name: xeus-python-kernel
channels:
  - https://repo.mamba.pm/emscripten-forge
  - conda-forge
dependencies:
  - xeus-python
  - ipycanvas
  - pip:
    - ipyparams  # only works in Jupyter, not JupyterLab
    - anywidget  # try https://github.com/jupyterlite/jupyterlite/discussions/1090#discussioncomment-6802901
